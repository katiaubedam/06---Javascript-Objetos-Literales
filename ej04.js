console.log("board: " + randomObject.them.speed.past.program.grade.addition.whose.national.cannot.board)

let input = prompt("Introduce algo:")

randomObject.them.speed.past.program.grade.addition.whose.national.cannot.name = input

console.log("name: " + randomObject.them.speed.past.program.grade.addition.whose.national.cannot.name)